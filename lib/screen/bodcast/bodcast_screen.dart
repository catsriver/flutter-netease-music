import 'package:flutter/material.dart';

class BodcastScreen extends StatelessWidget {
  const BodcastScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('播客'),
      ),
    );
  }
}
